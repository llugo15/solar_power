import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.MySolarCharge',
  appName: 'MySolarCharge',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
